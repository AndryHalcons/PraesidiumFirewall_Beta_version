#squid.conf

# SSL bump certificate generator
sslcrtd_program /usr/lib/squid/security_file_certgen -s /var/lib/ssl_db -M 4MB
sslcrtd_children 5

# Only allow cachemgr access from localhost
http_access allow localhost manager
http_access deny manager

# This default configuration only allows localhost requests because a more
# permissive Squid installation could introduce new attack vectors into the
# network by proxying external TCP connections to unprotected services.
http_access allow localhost

# The two deny rules below are unnecessary in this default configuration
# because they are followed by a "deny all" rule. However, they may become
# critically important when you start allowing external requests below them.

# Protect web applications running on the same server as Squid. They often
# assume that only local users can access them at "localhost" ports.
http_access deny to_localhost

# Protect cloud servers that provide local users with sensitive info about
# their server via certain well-known link-local (a.k.a. APIPA) addresses.
http_access deny to_linklocal

#
# INSERT YOUR OWN RULE(S) HERE TO ALLOW ACCESS FROM YOUR CLIENTS
#
#include /etc/squid/conf.d/*.conf




#NO permit cache
#
cache deny all
refresh_pattern . 0 0% 0


##profiles.conf
acl myprofile2 dstdomain  "/etc/squid/conf.d/domain_list/example_list.txt"
acl myprofile1 dstdomain  "/etc/squid/conf.d/domain_list/example_list.txt"


## SSL bump.conf 
# Definir paso 1 del handshake TLS
acl step1 at_step SslBump1
# Opciones TLS para el tráfico interceptado
sslproxy_options NO_SSLv3,SINGLE_DH_USE,SINGLE_ECDH_USE
sslproxy_cert_error allow all
sslproxy_flags DONT_VERIFY_PEER

# Forzar salida directa al destino original en intercept
always_direct allow all

# Paso 1: mirar el SNI solo en el primer paso
ssl_bump peek step1

# Paso 2: forzar bump en todo lo demás
ssl_bump bump all




##listen_ports.conf
#http_port 3128
http_port 3128 ssl-bump cert=/etc/squid/conf.d/certs/praesidium_issuer_ca.pem key=/etc/squid/conf.d/certs/praesidium_issuer_ca.key
#http_port 3129 intercept ssl-bump cert=/etc/squid/conf.d/certs/praesidium_server.pem key=/etc/squid/conf.d/certs/praesidium_server.key
http_port 3129 intercept ssl-bump generate-host-certificates=on dynamic_cert_mem_cache_size=4MB cert=/etc/squid/conf.d/certs/praesidium_issuer_ca.pem key=/etc/squid/conf.d/certs/praesidium_issuer_ca.key
#http_port 3129 intercept ssl-bump generate-host-certificates=on dynamic_cert_mem_cache_size=4MB cert=/etc/squid/conf.d/certs/praesidium_server.pem key=/etc/squid/conf.d/certs/praesidium_server.key




##safe_ports.conf
acl SSL_ports port 443
acl Safe_ports port 80          # http
acl Safe_ports port 21          # ftp
acl Safe_ports port 443         # https
acl Safe_ports port 70          # gopher
acl Safe_ports port 210         # wais
acl Safe_ports port 1025-65535  # unregistered ports
acl Safe_ports port 280         # http-mgmt
acl Safe_ports port 488         # gss-http
acl Safe_ports port 591         # filemaker
acl Safe_ports port 777         # multiling http


##localnet.conf
acl localnet src 0.0.0.1-0.255.255.255  # RFC 1122 "this" network (LAN)
acl localnet src 10.0.0.0/8             # RFC 1918 local private network (LAN)
acl localnet src 100.64.0.0/10          # RFC 6598 shared address space (CGN)
acl localnet src 169.254.0.0/16         # RFC 3927 link-local (directly plugged) machines
acl localnet src 172.16.0.0/12          # RFC 1918 local private network (LAN)
acl localnet src 192.168.0.0/16         # RFC 1918 local private network (LAN)
acl localnet src fc00::/7               # RFC 4193 local private network range
acl localnet src fe80::/10              # RFC 4291 link-local (directly plugged) machines
acl to_localhost dst 127.0.0.0/8 ::1
acl to_linklocal dst 169.254.0.0/16
acl myiplist src "/etc/squid/conf.d/ip_list/myiplist.txt"




##policies.conf
# Deny requests to certain unsafe ports
http_access deny !Safe_ports

# Deny CONNECT to other than secure SSL ports
http_access deny CONNECT !SSL_ports

#policies
http_access deny myprofile1 myiplist   
http_access deny myprofile2 myiplist    
http_access allow all


